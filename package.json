{
  "name": "ai-image-generator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "node server/index.mjs --api-only",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "node server/index.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
